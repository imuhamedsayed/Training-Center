@model IEnumerable<TrainingCenter.BLL.Models.InstructorVM>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/MainLayout.cshtml";
}

<br />
<br />
<a asp-action="Create" asp-controller="Instructor" class="btn btn-primary">+ Add New Instructor</a>

<br />
<br />

<table class="table table-striped table-hover text-center">

    <thead>
        <tr>
            <th class="text-center">Name</th>
            <th class="text-center">Email</th>
            <th class="text-center">Salary</th>

            <th class="text-center">Operations</th>

        </tr>
    </thead>
    <tbody>



        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Email</td>
                <td>@item.Salary</td>

                <td>

                    <a asp-action="Edit" asp-controller="Instructor" asp-route-id="@item.Id" class="btn btn-info">Update</a>
                    <a asp-action="Delete" asp-controller="Instructor" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>

                </td>
            </tr>
        }

    </tbody>
</table>
